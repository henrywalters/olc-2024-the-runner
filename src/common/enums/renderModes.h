//
// Created by henry on 2/29/24.
//

#ifndef HABAN_RENDERMODES_H
#define HABAN_RENDERMODES_H

#include <hagame/utils/enum.h>

ENUM(RenderMode)
ENUM_VALUE(RenderMode, Color)
ENUM_VALUE(RenderMode, Lighting)
ENUM_VALUE(RenderMode, Debug)
ENUM_VALUE(RenderMode, UI)
ENUM_VALUE(RenderMode, Combined)

#endif //HABAN_RENDERMODES_H
